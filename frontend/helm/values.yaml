fullnameOverride: newslingo-frontend
newslingoFrontend:
  image:
    repository: localhost:5000/newslingo-frontend
    tag: latest
  ports:
  - port: 80
    targetPort: 80
  replicas: 1
  type: ClusterIP
storage:
  imagesPath: "/mnt/newslingo/images"  # 실제 배포 시 적절한 경로로 변경하세요
  audioPath: "/mnt/newslingo/tts"      # 실제 배포 시 적절한 경로로 변경하세요
